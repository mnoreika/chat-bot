To run the bot type in terminal:

python3 bot.py

If you want to see the estimated category of the input as well use the -c flag:

python3 bot.py -c

The text corpus has been checked for curse words and is filtered.
